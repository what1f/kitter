[package]
name = "ztracing"
version = "0.1.0"
edition = "2024"
description = "No-op tracing compatibility shim for Kitter's GPUI build"
license = "Apache-2.0"
publish = false

[lib]
proc-macro = true
